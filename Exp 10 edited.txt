Code
#Step 1: Import Libraries
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

#Step 2: Load Dataset
df = sns.load_dataset('iris')

df.head()

#Step 3: Basic Information
print("Shape:", df.shape)
print(df.dtypes)

#Step 4: List Feature Types
numeric_features = df.select_dtypes(include=['float64', 'int64']).columns
categorical_features = df.select_dtypes(include=['object']).columns

print("Numeric Features:", numeric_features)
print("Categorical Features:", categorical_features)

Expected:

Numeric:

sepal_length
sepal_width
petal_length
petal_width

Categorical:

species


#Step 5: Histogram for Each Feature
for column in numeric_features:
    plt.figure(figsize=(6,4))
    plt.hist(df[column], bins=10)
    plt.title(f'Histogram of {column}')
    plt.xlabel(column)
    plt.ylabel('Frequency')
    plt.show()


#Step 6: Boxplot for Each Feature
for column in numeric_features:
    plt.figure(figsize=(6,4))
    sns.boxplot(x=df[column])
    plt.title(f'Boxplot of {column}')
    plt.show()


#Step 7: Compare Distributions and Identify Outliers

This is the missing part.

Species-wise Comparison
for column in numeric_features:
    plt.figure(figsize=(6,4))
    sns.boxplot(x='species', y=column, data=df)
    plt.title(f'{column} Distribution by Species')
    plt.show()